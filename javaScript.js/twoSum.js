function twoSum(numbers, target) {
    let numf = numbers[0];
    let num = numbers[numbers.length - 1];
    let rusult =num + numf
    return rusult;
}

let arra = [2, 3, 1];
console.log(twoSum(arra,2));